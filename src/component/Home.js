import React from 'react';
import Identity from './Identity';
import Navbar from './Navbar';

const Home = () => {
    return (
        <div>
           <Navbar></Navbar> 
           <Identity></Identity>
        </div>
    );
};

export default Home;